<%= render partial: "layouts/menu" %>
<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>User name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.user_name %></td>
        <td><%= link_to 'Ver', user, class: 'btn btn-primary', remote: 'true' %></td>
        <td><%= link_to 'Editar', edit_user_path(user), class: 'btn btn-warning' %></td>
        <td><%= link_to 'Eliminar', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path, class: 'btn btn-success' %>

<br/><br/>

<div class="table-responsive">
	<table class="table">
		<tr>
			<td>
				<div class="dropdown">
					<button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
						Ejemplo 1
						<span class="caret"></span>
					</button>
					<ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
						<li><%= link_to 'Opción 1', pagina_principal_path, remote: 'true' %></li>
						<li><%= link_to 'Opción 2', twomodal_index_path, remote: 'true' %></li>
						<li><%= link_to 'Opción 3', threemodal_index_path, remote: 'true' %></a></li>
						<li role="separator" class="divider"></li>
						<li><a href="#">Especial 1</a></li>
						<li><a href="#">Especial 2</a></li>
  					</ul>
				</div>
			</td>
			<td>
				<p>Contenido dinámico</p>
				<div id="contenedor"></div>
			</td>
			
		</tr>
	</table>
</div>

<div id="show-modal" class="modal fade"> 
	<div class="modal-dialog">
		<div id="inner-show-modal" class="modal-content">     
		</div>
	</div>
</div>

